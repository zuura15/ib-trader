# SEED ONLY — the live watchlist is stored in Redis (watchlist:symbols)
# and edited via the UI / API. This file seeds Redis on first run (when
# the key is absent); after that Redis is authoritative and this file is
# never written at runtime, so manual edits never conflict with git pull.
#
# Futures chart contracts are NOT listed here: the engine auto-anchors
# its chart-bot-* symbols (config/bots/*.yaml) so their quote subs never
# lapse, independent of the operator watchlist. See
# ib_trader/config/watchlist_runtime.py.
- QQQ
- GLD
- META
- CRM
- USO
- NVDA
- SPY
- GOOGL
- AMZN

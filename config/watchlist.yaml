- QQQ
- GLD
- META
- CRM
- USO
- NVDA
- SPY
- AAPL
- MSFT
- GOOGL
- AMZN
- MESM6
- MGCQ6
# GCQ6 (full Gold) pinned 2026-06-09 — chart-bot-1 displays it but
# the operator doesn't always hold a GC position, so without a
# watchlist anchor the engine's IB sub lapses (~2 min after the
# last GC position closed) and the chart freezes. ESM6 doesn't
# need this because the operator currently holds ES.
- GCQ6
# NQM6 (full Nasdaq) pinned 2026-06-09 — chart-bot-4 symbol. Its IB
# quote sub was anchored only by the held position, which lapsed and
# left the quote stream 17 min stale while the chart sat frozen.
# Watchlist-pin keeps it streaming regardless of position state, same
# rationale as GCQ6 above. (Until chart panes self-subscribe their
# own symbols, watchlist is the durable anchor.)
- NQM6
# MNQM6 (micro Nasdaq, June) pinned 2026-06-10 — chart-bot-3 symbol.
# Switched from NQU6: operator needs NQ exposure on this pane but
# U6's back-month volume was too thin to scan. Micro front-month is
# liquid. Pinned so its quote sub doesn't lapse when flat.
- MNQM6
